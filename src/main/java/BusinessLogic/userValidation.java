package BusinessLogic;

/**
 * Created by ${Boris} Grunwald} on 25/11/2016.
 */
public class userValidation {

    //Returns 0 if user is member, 1 if user is admin and -1 if user is not in DB

   /* public static int isUser(String username, String password) {

        //if(username is in db) {
            //String hash = gethash
            if(PasswordStorage.verifyPassword(password,hash));
                if()
    }
*/

}
